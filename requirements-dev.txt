# Test-only dependencies — not needed to run the app, only to run the
# test suite. Kept separate from requirements.txt so production installs
# (and install.ps1) don't pull in tooling they'll never use.
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
